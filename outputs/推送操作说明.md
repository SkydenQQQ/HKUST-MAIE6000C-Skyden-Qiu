# Week 3 推送操作说明

姓名：QIU GuanSheng；学号：21320398。

本地代码已经提交到 `fix/week03-test-discovery` 分支，提交简称为 `efa48c5`。
这不是最终 Canvas checkpoint SHA；最终 SHA 应从合并后的标签读取。
当前尚未推送、创建 PR、合并或创建标签。

## 原因

GitHub 写入接口返回 `403 Resource not accessible by integration`。
这是当前连接凭据权限不足；将仓库设为 public 只解决了公开读取，不能授予写入权限。
官方说明：https://docs.github.com/en/rest/using-the-rest-api/troubleshooting-the-rest-api#resource-not-accessible

## 方法一：修复现有 GitHub 应用授权

1. 登录 SkydenQQQ，打开 https://github.com/settings/installations 。
2. 找到本次连接使用的应用，点击 Configure。
3. 检查 Repository access 是否包含 `HKUST-MAIE6000C-Skyden-Qiu`，缺少时添加并保存。
4. 查看 Permissions。写代码需要 Contents 写权限，创建 PR 需要 Pull requests 写权限。
   如果应用本身只请求了读取权限，仅增加仓库选择仍不能推送；需要支持写入的连接或使用下面的本机 Git 方式。
5. 完成后告诉助手重试。你已批准推送、CI 通过后合并和创建标签，不需要重复批准。

## 方法二：在你自己的 PowerShell 中登录并推送

如尚未安装，先安装 Git for Windows（https://git-scm.com/download/win）和 GitHub CLI（https://cli.github.com/），然后重新打开 PowerShell。

逐条运行；如果某一步报错，先停止，不要继续下一步。

```powershell
gh auth login --hostname github.com --git-protocol https --web
gh auth setup-git --hostname github.com
```

在浏览器中登录 SkydenQQQ 并完成授权。密码、验证码或令牌不需要发给助手。
登录说明：https://cli.github.com/manual/gh_auth_login

然后推送已经准备好的本地提交：

```powershell
$RepoPath = 'C:\Users\49737\Documents\Codex\2026-09-22\activity-5\work\repo'
git -c safe.directory="$RepoPath" -C "$RepoPath" status
git -c safe.directory="$RepoPath" -C "$RepoPath" push -u origin fix/week03-test-discovery
```

`safe.directory` 只对这次命令认可该工作目录，处理隔离环境创建目录造成的所有者差异，不修改全局设置。
如果提示分支已有新提交或不能 fast-forward，请不要强制推送，把错误发给助手处理。

推送成功后可告诉助手继续，或自行完成下面的 PR 步骤：

1. 打开 https://github.com/SkydenQQQ/HKUST-MAIE6000C-Skyden-Qiu/compare/main...fix/week03-test-discovery 。
2. 创建 PR，标题为 `Fix W03 test discovery and prepare readiness submission`。
3. 描述说明：修正测试文件名以恢复两个遗漏测试；补齐 Activity 5 Dockerfile；新增提交文档和验证记录。本地 6 个单元/集成测试通过，SQLite 实际服务环境中完整 7 个测试通过。Docker/Compose 尚未验证。
4. 等 GitHub 的 `ci / test` 完成且通过后，才合并到 main。如果失败，先修复。
5. 告诉助手 PR 链接，便于核验合并结果并生成最终 Canvas 文本。

## 合并后创建标签（仅在检查通过并合并后执行）

```powershell
git -c safe.directory="$RepoPath" -C "$RepoPath" fetch origin main
git -c safe.directory="$RepoPath" -C "$RepoPath" tag --list w03-readiness
```

如果已存在同名标签，停止并先核对它指向的提交，不要覆盖。
确认不存在后执行：

```powershell
git -c safe.directory="$RepoPath" -c user.name='QIU GuanSheng' -c user.email='125984501+SkydenQQQ@users.noreply.github.com' -C "$RepoPath" tag -a w03-readiness origin/main -m 'W03 Individual Readiness Lab'
git -c safe.directory="$RepoPath" -C "$RepoPath" push origin refs/tags/w03-readiness
git -c safe.directory="$RepoPath" -C "$RepoPath" rev-parse 'w03-readiness^{commit}'
```

将最后输出的完整 SHA 发给助手，核验远端标签后补入 Canvas 草稿。
README 已删除 AI Use Statement 第五节；按作业要求，Canvas 文本仍保留 AI 使用声明。
